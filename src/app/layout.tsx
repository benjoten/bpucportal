import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "BPUC FY 26-27 - Hanuman Jayanti Payment Update",
  description: "BPUC FY 26-27 Hanuman Jayanti Payment Management System - Update payments in Google Sheets",
  keywords: ["BPUC", "Hanuman Jayanti", "Payment", "Google Sheets", "Management"],
  authors: [{ name: "BPUC Team" }],
  icons: {
    icon: "https://z-cdn.chatglm.cn/z-ai/static/logo.svg",
  },
  openGraph: {
    title: "BPUC FY 26-27 - Hanuman Jayanti Payment Update",
    description: "BPUC FY 26-27 Hanuman Jayanti Payment Management System",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
